fn main() {
    // Параллелизм (--test-threads)
    // Вывод текста из теста (--show-output)
    // Запуск одного теста
    // Запуск нескольких тестов
    // Игнорирование тестов
    // Запуск только проигнорированных тестов (--ignored)
    // Запуск тестов с проигнорированными тестами (--include-ignored)
}